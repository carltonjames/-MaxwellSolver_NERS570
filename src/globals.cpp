#include "globals.hpp"
#include <cmath>

double delta = 0.0;
const float timeStep = 0.001f; // sec
const float c = 2.9979f * powf(10.0f, 10.0f); // cm/sec
const float e = 4.8029f * powf(10.0f, -10.0f); // statcoloumbs
const float m_e = 9.1083f * powf(10.0f, -13.0f); // MeV-cm
